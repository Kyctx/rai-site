---
// Кнопка (UI Kit «Рай»): variant Primary/Dark/Outline/Soft, size M/L.
// Primary — основное действие, остальные — второстепенные (см. описание компонента в Figma).
import Icon from '@components/Icon.astro';

interface Props {
  variant?: 'primary' | 'dark' | 'outline' | 'soft';
  size?: 'm' | 'l';
  icon?: string;
  iconPosition?: 'start' | 'end';
  href?: string;
  type?: 'button' | 'submit';
  class?: string;
  [key: `data-${string}`]: string | undefined;
}

const {
  variant = 'primary',
  size = 'm',
  icon,
  iconPosition = 'end',
  href,
  type = 'button',
  class: className,
  ...rest
} = Astro.props;

const Tag = href ? 'a' : 'button';
const iconSize = 20;
---

<Tag
  href={href}
  type={!href ? type : undefined}
  class:list={['btn', `btn--${variant}`, `btn--${size}`, 'pill-hover', className]}
  {...rest}
>
  {icon && iconPosition === 'start' && <Icon name={icon} size={iconSize} />}
  <span class="label-m"><slot /></span>
  {icon && iconPosition === 'end' && <Icon name={icon} size={iconSize} />}
</Tag>

<style>
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-12);
    border-radius: var(--radius-pill);
    white-space: nowrap;
    position: relative;
    overflow: hidden;
    z-index: 0;
    transition: box-shadow 0.3s cubic-bezier(0.23, 1, 0.32, 1), color 0.3s ease, border-color 0.3s ease;
  }

  .btn :global(svg) {
    flex-shrink: 0;
  }

  /* Круг растёт из точки в центре кнопки. height: 100% + aspect-ratio: 1
     держит его настоящим кругом (сторона = высоте кнопки) на любой ширине —
     в отличие от inset: 0 без aspect-ratio, который давал эллипс, повторяющий
     пропорции самой кнопки. scale(0) сжимает круг в точку в центре, scale(6)
     на hover раскрывает с запасом даже самую широкую кнопку сайта
     (кнопка-телефон в хедере, ratio ширина:высота ≈ 4). */
  .btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    translate: -50% 0;
    height: 100%;
    aspect-ratio: 1;
    border-radius: 50%;
    scale: 0;
    z-index: -1;
    transition: scale 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .btn:hover::before {
    scale: 6;
  }

  @media (prefers-reduced-motion: reduce) {
    .btn,
    .btn::before {
      transition: none;
    }
  }

  .btn--m {
    padding: var(--space-16) var(--space-32);
  }

  .btn--l {
    padding: var(--space-20) var(--space-40);
  }

  .btn--primary {
    background: var(--color-accent-primary);
    color: var(--color-text-inverse);
  }

  .btn--primary::before {
    background-color: var(--color-accent-hover);
  }

  .btn--primary:hover {
    color: var(--color-text-inverse);
    box-shadow: 0 0 20px rgba(86, 99, 75, 0.4);
  }

  .btn--dark {
    background: var(--color-bg-inverse);
    color: var(--color-text-inverse);
  }

  .btn--dark::before {
    background-color: var(--color-accent-primary);
  }

  .btn--dark:hover {
    color: var(--color-text-inverse);
    box-shadow: 0 0 20px rgba(86, 99, 75, 0.4);
  }

  .btn--outline {
    background: var(--color-bg-surface);
    color: var(--color-text-primary);
    border: 1px solid var(--color-border-default);
  }

  /* Заливка красит только рамку (stroke), не фон и не текст кнопки */
  .btn--outline::before {
    display: none;
  }

  .btn--outline:hover {
    border-color: var(--color-accent-primary);
  }

  .btn--soft {
    background: var(--color-accent-soft);
    color: var(--color-accent-primary);
  }

  .btn--soft::before {
    background-color: var(--color-accent-primary);
  }

  .btn--soft:hover {
    color: var(--color-text-inverse);
    box-shadow: 0 0 20px rgba(86, 99, 75, 0.4);
  }
</style>
