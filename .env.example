# Публичные переменные окружения (доступны в браузере, начинаются с PUBLIC_).
# Скопируйте в .env, .env не коммитить.

# Счётчик Яндекс Метрики — подключается только в production (см. CLAUDE.md, раздел «Аналитика»).
PUBLIC_YANDEX_METRIKA_ID=
